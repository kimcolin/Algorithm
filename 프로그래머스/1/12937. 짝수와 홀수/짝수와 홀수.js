function solution(num) {
    let answer = '';
        if (num % 2 === 0) //정수를 2로 나눈 나머지가 0일 경우 짝수이고, 0이 아닐 경우 홀수이다.
            return answer = "Even" // 나머지가 0이라면 "Even"
        else {
            return answer = "Odd" // 그게 아니라면 "Odd"
        }
    return answer;
}